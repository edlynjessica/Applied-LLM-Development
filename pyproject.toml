[project]
name = "eec"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "edlynjessica", email = "edlynjessica09@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "langchain>=1.3.14",
    "langchain-openai>=1.4.1",
]

[project.scripts]
eec = "eec:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
